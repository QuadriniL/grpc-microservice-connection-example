syntax = "proto3";

message AuthRequest {
  string username = 1;
  string password = 2;
}

message AuthResponse {
  string token = 1;
  string error = 2;
}

service AuthService {
  rpc Login(AuthRequest) returns (AuthResponse);
}